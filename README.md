# Simple Subdomain Scanner in Pythonc

A basic multithreaded subdomain enumeration tool built using Python and ThreadPoolExecutor.

## Usage

python scanner.py --d example.com --w wordlist.txt --threads 20

## Features
- Multithreaded scanning
- Status code differentiation
- Basic error handling

